#!/usr/bin/env bash

if [ -z "${HZ_CMD_BIN}" ] ; then
    export HZ_CMD_BIN=~/hzCmd/target
fi

activemq start > /dev/null

ops="-Xms2g -Xmx2g"
java -cp "${HZ_CMD_BIN}/*:${HZ_CMD_BIN}/dependency/*" ${ops} main.HzCmd $@