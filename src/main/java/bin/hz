#!/usr/bin/env bash
#from scr

if [ -z "${HZ_CLUSTER_BIN}" ] ; then
    export HZ_CLUSTER_BIN=~/hz-script/target
fi

ops="-Xms32m -Xmx128m"


java -cp "${HZ_CLUSTER_BIN}/*:${HZ_CLUSTER_BIN}/lib/*" ${ops} local.Controler $@