package global;

public enum  Args{
    exit, install, uninstall, user, init, load, invoke, stop, ssh, info, membersOnly, members, clients,
    msg, sleep, homeUser, homeIp, homeCwd, homeInfile, clean, cat, ID, grep, layout, jps, cluster, addip, kill
}

