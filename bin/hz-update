#!/usr/bin/env bash

if [ -n "$1" ]; then
  git -C "${HZ_CMD_SRC}" checkout $1
fi

git -C "${HZ_CMD_SRC}" branch

git -C "${HZ_CMD_SRC}" pull

mvn -f "${HZ_CMD_SRC}" clean install dependency:copy-dependencies -DskipTest